
-- mostrar (SELECT) todos (*) lo que hay en (FROM) la tabla (users)
SELECT * FROM users 

-- mostrar todos los datos que hay en la columna "name" de la tabla "users"
SELECT name FROM users

-- mostrar todos los datos que hay en la columna "user_id" y la columna "name" de la tabla "users"
SELECT user_id, name FROM users